import { logo } from "../../../assets/assets";

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} width="80" height="30"></img>
    </div>
  );
};

export default Logo;
